{
  "name": "cep-monorepo",
  "private": true,
  "workspaces": [],
  "scripts": {
    "postinstall": "npm install --prefix frontend --include=dev",
    "build": "npm run build --prefix frontend",
    "start": "npm run preview --prefix frontend",
    "lint": "npm run lint --prefix frontend || exit 0"
  }
}

