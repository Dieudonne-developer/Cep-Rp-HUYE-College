services:
  - type: web
    name: cep-backend
    env: docker
    rootDir: backend
    plan: free
    dockerfilePath: Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        sync: false
        # Set this in Render Dashboard: mongodb://mongo:LTKOiNBFniFluuStCilpHhYBSvRYgPMu@nozomi.proxy.rlwy.net:30110
      - key: CLIENT_ORIGIN
        fromService:
          type: web
          name: cep-frontend
          property: url
      - key: BACKEND_URL
        fromService:
          type: web
          name: cep-backend
          property: url

  - type: web
    name: cep-frontend
    env: docker
    rootDir: frontend
    plan: free
    dockerfilePath: Dockerfile
    # CRITICAL: Do NOT set staticPublishPath - this makes it a static site!
    # staticPublishPath should NOT be here for web service
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: VITE_API_BASE_URL
        fromService:
          type: web
          name: cep-backend
          property: url

